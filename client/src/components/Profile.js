import { useState } from 'react';

function Profile() {
  const [thiss, setThis] = useState(0);

  return (
    <div className="Profile">
      <p>Profile</p>
    </div>
  );
}

export default Profile;
